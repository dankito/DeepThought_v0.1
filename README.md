To init submodules clone and run

git submodule init
git submodule update

or simply clone with

git clone --recursive https://github.com/dankito/DeepThought.git


Adjust line
    ndkDir = /opt/android-ndk-r10c/
in gradle.properties