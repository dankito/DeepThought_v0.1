<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             tools:context="net.deepthought.dialogs.RegisterUserDevicesDialog">

    <Spinner
        android:id="@+id/spnDeviceRegistrationOptions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        />

    <ListView
        android:id="@+id/lstvwRegisteredDevices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@id/spnDeviceRegistrationOptions"
        android:layout_above="@+id/btnStartStop"
        />

    <Button
        android:id="@+id/btnStartStop"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dialog_button_height"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:minWidth="140dp"
        android:layout_marginLeft="4dp"
        android:enabled="false"
        android:text="@string/start"
        />

    <Button
        android:id="@+id/btnClose"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dialog_button_height"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@id/btnStartStop"
        android:minWidth="140dp"
        android:layout_marginRight="4dp"
        android:text="@string/close"
        />

</RelativeLayout>
